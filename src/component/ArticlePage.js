import React from "react";

const ArticlePage = () => {
    <React.Fragment>
        <header>
            <button> Back </button>
        </header>
        <main></main>
    </React.Fragment>;
};

export default ArticlePage;
