import React from "react";

const Header = () => {
    return (
        <header>
            <h1>Hacker News</h1>
            <button>Update</button>
        </header>
    );
};

export default Header;
